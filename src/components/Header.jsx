import React from 'react';

const Header = (props) => {
    return(
        <header className="navbar bg-info text-white">
            <h1>Reacting to API's</h1>
        </header>
    );
}

export default Header;